#include <stdlib.h>
#include <stdio.h>

/**
 * mod - returns modulus of two integers
 * @a: first integer
 * @b: second integer
 * Return: modulus of the two integers
 */
int mod(int a, int b)
{
	return (a % b);
}
